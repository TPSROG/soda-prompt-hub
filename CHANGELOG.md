# 更新记录

本文件记录 Soda Prompt Hub 面向使用者的重要变化。开发过程和历史验收细节只保存在维护者本机，
不进入公开仓库和发行包。

## [未发布]

### Linux 支持（实验性，非官方构建）

- 新增 `deploy/linux/`：安装、卸载、更新、启停、状态脚本与 systemd **用户**服务。
  不需要 `sudo`，服务以当前用户运行，默认只监听 `127.0.0.1:8765`。
- 新增 `soda-prompt-hub` 便利命令：`start` / `stop` / `restart` / `status` / `logs` / `serve` / `update` / `version`。
- 新增 `tests/linux/`：部署层静态契约测试，以及使用隔离 `HOME` / XDG 路径的 Linux 行为测试。
- 新增 `.github/workflows/linux.yml`：Ubuntu 22.04 与 24.04 完整测试、部署冒烟、systemd 用户服务验收。
- 固定维护分支使用 `linux/main`；上游同步与发行暂由维护者手动执行，不在非默认分支放置无法触发的
  `schedule` / `workflow_run` 工作流。
- 新增 `scripts/linux/check-env.sh` 环境自检；Linux 文档见 `docs/linux/`。
- 新增 Linux Compute Worker（实验性）：`install.sh --with-worker` 准备桥接目录、`soda-worker` 命令与
  systemd 单元；Worker 从桥接目录领取任务、调用 ComfyUI HTTP API 出图并回传带 sha256 的结果。
  协议层已在 Linux 上端到端验证（真 Worker + 真桥接 + 假 ComfyUI，无需 GPU）；LoRA 正式训练仍仅限 Windows。
- 修复：`install.sh --no-service` 打印的直接运行命令此前漏掉 `PROMPT_HUB_MODELS_ROOT`。
- 新增 Linux 本机使用模式（`linux_local`）：Core 与 Compute Worker 在同一台机器上通过本地桥接目录协作，
  界面文案、示例路径与配对入口按平台区分；`install.sh --with-worker` 会登记本机计算节点，设备页直接可用。
- ComfyUI 地址不再限制为本机：支持 `http(s)` 任意主机，并支持 `https://用户名:密码@主机` 形式的 HTTP Basic 认证。
- Linux 部署安全加固：卸载前解析真实路径，systemd unit 支持空格和 `%` 路径，Worker 配置仅当前用户可读，
  更新时同步刷新 Core / Worker unit 并重启正在运行的 Worker。
- 已知限制：LoRA 正式训练仍仅限 Windows；SMB 双机配对在 Linux 上用本地桥接目录代替。

### 1.1.1 预发布修复

- Windows 数据集交付“打开所在文件夹”改用 `explorer.exe`；macOS 与 Linux 使用各自文件管理器，并把启动失败转为可读错误。
- 完整个人资料备份显式关闭 SQLite 连接，避免 Windows 文件锁；资料管理页增加大小与空间估算、后台进度、取消和自动校验。
- 图片详情把整段译文明确命名为“Krea 2 中文翻译（仅供审核）”；视觉草稿为空时自动改用正式 Krea 2 英文说明，仍不进入交付文件。
- Windows 出图结果保存手动导入、目录扫描、Worker 回传的来源、路径和批次；可记住常用结果目录并按来源筛选。
- 未关联结果可安全移出结果库；源目录原图不删除，Prompt Hub 管理副本进入内部回收区。有关联的结果会拒绝移出。
- 设备任务页自动轮询活动任务，取消记录显示取消时间与原因；服务重启后的后台取消状态也会归一为“已取消”。

当前作为 `1.1.1` 第一版预发布候选，未完成签名与高成本完整验收，不应标为正式 Latest。

## [1.1.0 桌面验收构建] - 2026-09-13

- [Mac + Windows Worker](https://github.com/cOkieeman/soda-prompt-hub/releases/tag/v1.1.0-mac-windows-20260913)
- [Windows 单机版](https://github.com/cOkieeman/soda-prompt-hub/releases/tag/v1.1.0-windows-standalone-20260913)

两套对应源码 `5f5e885`，软件版本仍为1.1.0，GitHub状态为Pre-release；未正式平台签名，完整新包原生手验待完成。
自动化测试及构建校验不等于完整人工验收，操作清单见[手动验收](docs/acceptance/manual-1.1.0-20260913.md)。

### 新增

- 提供 Mac 自包含 DMG、Windows Desktop 安装器和 Windows Compute Worker 安装器；支持
  Mac 管理 Windows 与 Windows 单机两种使用方式，无须手工安装 Python 或 `uv`。
- 三端增加统一图标与启动台、托盘或菜单栏服务控制、日志入口和脱敏诊断包导出。
- 增加首次配对引导、本地模型可选安装界面，以及 OC Manager 角色内容带入创作项目的预览流程。
- 人工审核步骤增加「写入 Krea 2」批次按钮：把所选图片里已有的视觉草稿一次写入正式 Krea 2 说明，
  已经人工确认过的说明不会被覆盖，整批只留一条可回退的修改记录。
- 人工审核步骤增加「插入触发词」输入框：把填写的触发词插到所选图片当前格式说明的最前面，
  已经以它开头的图片不会重复插入。
- 人工审核步骤增加「批次翻译」：为所选图片的 Krea 2 英文说明批量生成中文对照，
  可取消、可续跑，译文只作对照，不改动英文说明，也不会进入交付版本。
- 人工审核步骤增加「标记说明已确认」：把所选图片当前格式的说明整批标记为人工确认，
  只改判断不改文字，空白说明不会被确认。

### 修复

- Windows 单机首屏不再闪现 Mac 配对界面；路径、错误提示与交付入口按模式适配，单机不显示跨设备复制。
- 修复窄屏模型服务工具栏按钮被挤成竖排的问题。
- Windows 源码打包时在生成哈希清单前规范化 PowerShell 脚本为 UTF-8 BOM，兼容 PowerShell 5.1 中文环境。
- Windows 安装包内置私有 Git；资料更新显示实际任务进度，个人数据备份不再依赖 Mac 专用 Git 路径。
- Windows embedded Python 优先加载包内 Core；修复单机启动等待、模型连接表单隐藏及启动器文字对比度问题。
- 结果目录扫描、模型清单和标签翻译增加阶段反馈；CLIP 发现阶段支持进度与协作式取消，保留已完成批次。
- 单图审核时无需改动原文即可确认非空说明；导入结果刷新画廊与旅程，旧页面保存不会覆盖已导入结果。
- 设备连接提示以实时 Worker 心跳为准；Mac 增加“退出并停止服务”，停止前核对服务进程身份。
- “让模型帮忙”更名为“AI补全”，提高输出额度并明确提示截断，不自动应用不完整建议。
- 程序与个人数据目录分离，覆盖安装保留资料库和配置；未签名分发仍需遵循各平台的安全提示。
- 插入触发词不再把已经人工确认的说明打回草稿：只在最前面插词、不改动其他内容时保留确认状态，
  改写内容的批量整理仍然需要重新确认。
- 数据集页面在扫描出近似重复图片时不再整页停止渲染：近似重复记录的是 `left_files` / `right_files`
  两边，之前按完全重复的 `files` 读取会抛错，导致五步流程、图片列表和人工审核全部点不动。
- 交付前检查重新能报出「选择中有近似重复图片」的提醒，之前因为同一个读取错误永远为空。

## [1.1.0] - 2026-09-09

### 新增

- 增加简体中文、繁体中文和英文界面，并记住当前设备的语言选择。
- 建立正式版本体系、首页版本信息、Mac 安全更新器和可独立校验的 Windows Worker 发布包。
- 数据集工作台支持通用、肖像、服装和风格四种说明文字模式，并可保存本机预设。
- Anima 可选择 WD SwinV2 v3、IdolSankaku Tagger 或已连接的视觉模型；Krea 2 支持中文对照、意见改写和逐图确认。
- 资料库支持按来源筛选和只看带图资料，并可将 AnimaDex 的角色、画师与作品缩略图作为视觉参照。
- LoRA 数据集在交付前分别检查 Anima 标签和 Krea 2 自然语言说明；两种格式均被选择时会生成两套独立目录。

### 修复与改进

- Windows 返回任务可单条或批量移入历史，旧清单任务会自动收起，减少任务页面堆叠。
- 改进 ComfyUI 参数读取，可保留跨行正向提示词和 `ImpactWildcardEncode` 展开后的完整 Prompt。
- 修复数据集来源目录消失、模型连接编辑状态残留和局域网 HTTP 模型服务无法保存的问题。
- 自动说明不再无条件添加写实媒材词；“打标模型”统一表示 WD14 或视觉模型的选择。
- 补齐三语界面的动态状态文案，并避免把本机路径或 Windows 主机名写入翻译表。
- Windows Worker 发布包改为可重复构建，固定文件时间与权限，同一源码和工具链可得到相同 SHA-256。
- LoRA 与底模目录继续保留 Windows 上的分类、预览图和 Civitai 来源，模型权重不会复制到 Mac。

### 升级说明

- 从旧版升级时使用新版源码中的 `更新-Soda-Prompt-Hub.command`；更新器会先备份个人资料和旧程序。
- Windows 端请替换为同一 Release 中的 `Soda-Prompt-Hub-Windows-Worker-1.1.0.zip`，再重新运行自检和 Worker。
- 旧 `/api/model-connections*` 接口已由 `/api/model-endpoints*` 取代；网页中保存的模型端点需要重新检查一次。

## [1.1.0rc2] - 2026-09-09

### 新增

- 数据集工作台增加通用、肖像、服装和风格四种说明文字模式，可按训练目标省略对应内容并添加触发词。
- Anima 草稿可按图片类型选择 WD SwinV2 v3 或 IdolSankaku Tagger，也可以使用已连接的视觉模型。
- Krea 2 草稿支持按需中文对照、根据中文意见改写，并在逐张检查时保留每张图片的译文。
- 说明文字设置可以在当前浏览器保存为预设，供后续数据集重复使用。
- 界面增加简体中文、繁体中文和英文切换，并记住本机选择。

### 修复

- 设备任务支持把已经处理过的 Windows 返回记录单条或批量移入历史，不会导入结果或删除返回文件。
- Krea 2 的 `ImpactWildcardEncode` 工作流可以从 `populated_text` 读取通配符展开后的完整 Prompt。
- Windows Worker ZIP 使用固定文件时间和权限；同一提交、同一工具链重复构建得到相同 SHA-256。
- 自动生成说明不再无条件追加 `photo, realistic`，而是按图片真实媒材决定是否保留媒材信息。
- 数据集来源目录消失时会立即说明原因，不再逐张失败到队列末尾。
- 模型接入不再因为编辑状态残留而覆盖另一组已保存服务。
- 局域网 IP 上的 OpenAI-compatible 模型服务可以使用 HTTP；公网服务仍要求 HTTPS。
- “打标模型”统一表示 WD14 或视觉模型的选择，避免与通用、肖像、服装和风格模式混淆。
- 补齐三语切换下的数据集状态、设备任务、模型目录与模型接入动态文案；模型名、LoRA 名和用户备注仍保留原文。
- 修复手机端设备页四个子页面名称互相挤压的问题，较长名称会在按钮内换行显示。
- 移除翻译表中误收录的个人目录与 Windows 主机名，改用不保存真实运行值的动态翻译规则。

### 仓库与发布

- 本机开发计划、调查记录和进度记录不再由 Git 跟踪，也不会进入公开仓库或发行包。
- 更新公开开发计划与 Windows Worker 可重复构建说明。

## [1.1.0rc1] - 2026-09-07

### 新增

- 建立程序版本、发布通道、数据结构版本与 Windows Worker 协议的统一版本信息。
- 首页显示当前程序和数据结构版本，设备诊断显示 Worker 版本及兼容状态。
- 增加 Mac 安全更新器，更新前备份个人资料和旧程序，初始化失败时自动恢复。
- 增加可校验的 Windows Worker 独立 ZIP、首次配置入口和升级顺序。
- 将公开文档分为快速开始、核心工作流、Mac、Windows、设备关系、排错和发布体系。

### 破坏性变更

- 外部模型配置升级为“端点 + 勾选模型”结构，旧 `/api/model-connections*` 路径已移除；
  请使用 `/api/model-endpoints*` 管理多组 OpenAI-compatible 端点。

## [1.0.0] - 2026-09-05

### 主要能力

- 完成从灵感、OC 和视觉参考到 Anima/Krea 2 双格式 Prompt 的创作流程。
- 接入本地提示词来源、网页来源卡、CLIP 以图找图与视觉参照索引。
- 完成 5060 Ti ComfyUI 远程出图、结果回流、LoRA/底模只读清单和预览。
- 完成数据集扫描、WD14 草稿、人工审核、版本冻结、哈希校验和 Windows 交付。
- 增加 LM Studio 本地优先及可选 OpenAI-compatible 外部模型连接。
- 完成个人事实数据备份、隔离恢复、任务恢复与中文操作引导。

### 安全与边界

- API Key 仅保存在本机私有配置中，网页与接口不返回明文。
- 公共资料、OC Manager 和外部数据集默认只读，导出使用独立版本目录。
- Prompt Hub 不负责启动 LoRA 训练；训练、正则和最终标签筛选继续在 Windows 工具中完成。

### 发布说明

- 软件包版本统一为 `1.0.0`，产品名称统一为 Soda Prompt Hub。
- 增加公开仓库自动检查流程。
- 代码采用 MIT License；第三方提示词、图片、模型和数据集仍遵循各自来源条款。
